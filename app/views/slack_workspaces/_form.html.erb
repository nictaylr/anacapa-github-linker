<%= form_with(model: slack_workspace, local: true) do |form| %>
  <% if slack_workspace.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(slack_workspace.errors.count, "error") %> prohibited this slack_workspace from being saved:</h2>

      <ul>
      <% slack_workspace.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
